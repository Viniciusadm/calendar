# Configuração do app lida em tempo de build pelo app/build.gradle.kts.
# Copie para env.properties (fora do git) e ajuste o endereço do backend.
# Precisa terminar com barra; se faltar, o build acrescenta.
BASE_URL=https://manage.isarchie.com.br/
